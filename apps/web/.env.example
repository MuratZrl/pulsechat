# GIPHY API key. Required for the GIF picker — without it, the picker shows
# a "not configured" message instead of fetching. Sign up for a free dev key
# at https://developers.giphy.com (a beta key is rate-limited but fine for
# local development).
NEXT_PUBLIC_GIPHY_API_KEY=""
